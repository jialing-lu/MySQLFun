@{
    ViewData["Title"] = "Home Page";
}

@model List<Bowler>
<hi>Bowlers</hi>

<ul>
    @foreach (var b in Model)
    {
    <li>@b.BowlerFirstName, @b.BowlerMiddleInit, @b.BowlerLastName, @b.BowlerAddress, @b.BowlerCity, @b.BowlerState, @b.BowlerZip, @b.BowlerPhoneNumber</li>
    }
</ul>